const initialState = {
  cryptoList: [],
  cryptoLoading: false,
};

const cryptoReducer = (state = initialState, { type, data }) => {
  switch (type) {
    default:
      return state;
  }
};

export default cryptoReducer;